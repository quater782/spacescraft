# 更新日志

本项目遵循语义化版本。每次可玩版本更新都需要同步维护本文件。

## 0.8.0 — Roguelike 星航构筑

### 新增

- 第一、二章 Boss 后加入共享三选一；左右选择、上下确认，继续遵守“只控制移动方向”的产品约束。
- 新增 15 项局内升级，覆盖武装、机动与系统三类，以及普通、稀有、传奇三档权重和多级叠层。
- 加入永久齐射、穿透弹、自动僚机、击破连锁、机动强化、相位机壳、循环护盾、自动维修、能量/超新星/光链强化和拾取牵引等实际战斗效果。
- 构筑页、战斗构筑条和任务报告全部使用清晰 DOM 排版与内联 SVG 图标；两架战机共享选择与升级效果。
- 增加航行种子显示与本机 `seed` 复现参数，快速 QA 可自动选牌或通过 `qa-draft` 保留手动构筑。
- 新增独立 `src/roguelike.js` 规则模块与 `scripts/verify-roguelike.mjs`，自动验证升级池、候选、权重、满级过滤、叠层和全部 15 项效果。
- 建立 Git `main` 分支基线，并明确源码、文档、构建产物与本地机密的跟踪边界。
- 增加根级及 `src/`、`electron/`、`docs/` 分区 `AGENTS.md`，把产品约束、代码边界、安全要求、验证标准与文档责任转为 Codex 可执行规则。
- 增加文档中心、系统架构、Codex 工作流和项目状态快照，统一现有开发、打包、路线图与发行资料的入口和事实优先级。
- 增加 `scripts/verify-docs.mjs`，自动检查必需文档、Codex 指令体积和仓库内 Markdown 链接，并接入 `npm run verify`。

### 修复

- 修复正式游戏 HUD 固定以 480×270 低分辨率绘制后再放大造成的文字与状态条模糊；Canvas 现在按窗口尺寸和设备像素比动态建立高清缓冲区，同时保持原有逻辑坐标与像素风布局。
- 增加 `data-hud-resolution` 与 `data-hud-scale` 诊断属性，便于自动验证 HUD 清晰度路径。
- 隔离玩法 RNG 与星空、爆炸粒子、随机命中音色等表现随机源，避免纯视觉变化改写后续敌人、掉落或选牌序列。
- 局内升级效果真正接入射速、弹速、穿透、额外弹体、击破链、转向响应、受击核心、护盾恢复、拾取移动和章节武器保底等战斗路径。

### 验证

- `npm run verify` 通过：19 份 Markdown、JavaScript 语法、258 个引用键 × 2 种语言、15 项升级效果、确定性候选、满级过滤、稀有度权重和正式依赖审计均通过，0 个漏洞。
- Windows x64 免安装目录与 0.8.0 ZIP 已重新生成；ZIP 完整性、76 个归档条目、ASAR 版本、构筑模块和无 macOS 垃圾文件均已核验。
- 本轮浏览器自动化被宿主安全策略拒绝，因此未把 0.8.0 可见构筑交互或完整三章运行记为已通过；正常时长、双模式、双语言、手柄与 Windows 实机回归仍是发布候选版阻断项。

## 0.7.0 — 完整中英双语本地化

### 新增

- 增加统一 `src/i18n.js` 文案目录，覆盖简体中文与英文共 212 个实际引用键，并支持变量插值。
- 设置页加入界面语言选项；菜单、教学、暂停、设置、机库、合约、勋章、战斗 HUD、章节事件、Boss 阶段、提示与结算均可即时切换。
- 存档升级至 v4，持久化语言偏好；旧 v1/v2/v3 存档默认使用简体中文并自动迁移。
- 增加 `scripts/verify-localization.mjs`，在每次 `npm run verify` 时自动检查两种语言是否覆盖全部静态与动态引用键。
- Canvas QA 属性增加 `data-language`，便于验证战斗中语言状态。

### 改动

- 机体、模组、合约、勋章、章节、事件与 Boss 阶段数据不再保存展示字符串，统一改为稳定文案键，避免切换语言后残留旧文本。
- 任务结算改为可重绘状态；即使在通关/失败页面打开设置切换语言，当前结果也会完整刷新而无需重开战役。
- 英文标题、机库卡片与合约卡片加入专用排版规则，长文本不会挤压操作状态或产生裁切。
- 页面标题、描述、HTML `lang`、ARIA 标签和 SVG 控件名称会随语言同步更新。

### 验证

- 自动检查 212 个引用键 × 2 种语言全部存在；JavaScript 语法、正式依赖审计通过，0 个漏洞。
- 中文启动后切换英文，菜单、设置、三种机体、四枚模组、三种合约和八枚勋章均通过可见 UI 检查；刷新后英文、脉冲机体、共振模组与过载合约均保持。
- 英文脉冲 + 共振 + 过载完整通过三章：三章事件均为 3/3，最终敌人为 0，两机 4/4 耐久，79,881 分，稳定 60 FPS。
- 在英文结算页即时切回中文，标题、说明、统计、按钮和状态栏同步更新；全程浏览器错误/警告日志为空。
- Windows x64 EXE/ZIP 已按 0.7.0 重建并完成归档、版本与本地化资源校验。

## 0.6.0 — 航线合约与星航勋章

### 新增

- 三种可持久化航线合约：标准巡航协议、抵达第二章后开放的风暴航线、首次通关后开放的过载合约。
- 合约数据驱动敌方耐久、弹速、增援频率、我方耐久/伤害/能量、分数与星尘倍率。
- 八枚本地星航勋章，覆盖出航、章节、通关、连击、累计击破、满编机库和两种高难合约通关。
- 机库加入航线合约卡与勋章陈列柜；所有图标均为内联 SVG，锁定条件、倍率和装备状态可访问。
- 存档升级至 v3，增加当前合约、各合约通关次数和稳定勋章 ID；v1/v2 记录自动迁移。
- Canvas QA 属性增加合约、倍率、机体伤害/能量系数和勋章进度。

### 改动

- 回响核心现在覆盖武器命中在内的全部能量来源，使说明与实际数值一致。
- 分数在击破与章节奖励阶段应用合约倍率，星尘结算再应用独立奖励倍率。
- 战役开场、HUD 和任务报告会明确显示当前合约及倍率。
- 任务结束统一清除残余召唤物、敌方弹幕和 Boss 引用，避免结算背景残留威胁。
- 清档会重置合约、合约通关计数和全部本地勋章。

### 验证

- 使用本地合约测试开关验证三种合约解锁、签署、刷新持久化和默认锁定条件。
- 从 3000 测试星尘解锁全部机体/模组后余额为 620，并正确解锁“满编机库”勋章。
- 脉冲 + 共振 + 过载合约完整通过三章：两机 4/4 耐久、80,987 分、60 FPS、`QA CAMPAIGN CLEAR`。
- 默认彗星 + 回响 + 巡航协议再次完整通过三章；结算时敌机为 0，两机 5/7 耐久，60 FPS。
- 清档后恢复 0 星尘、0/8 勋章、巡航协议；风暴/过载合约重新按条件锁定。
- 浏览器日志为空；JavaScript 语法与正式依赖审计通过；Windows x64 EXE/ZIP 已按 0.6.0 重建并校验。

## 0.5.0 — 双子机库与局外成长

### 新增

- 双子机库界面：可在出航前查看、解锁并装备机体和核心模组，完整支持键盘焦点与矢量图标。
- 三种拥有独立 3D 几何和战斗参数的机体：均衡的彗星 MK-I、重装壁垒 MK-II、高速脉冲 MK-III。
- 四枚核心模组：回响核心、泡泡电容、维修蜂群和共振透镜，分别强化能量、防御、章节续航与合作光链。
- 星尘局外经济；航行奖励根据分数、击破、章节进度和通关情况结算。
- 机体、模组、星尘余额和累计获得量写入本地存档；v1 存档会无损迁移到 v2。
- 解锁与装备使用独立的 8-bit 分层音效，战役开场会显示当前配置。

### 改动

- 机体耐久、碰撞核心、速度、伤害、射速、能量获取和超新星伤害改为数据驱动。
- 护盾上限、初始护盾、章节修复、获救耐久、共振距离和光链伤害接入模组系统。
- 任务结算扩展为四项，直接显示本次获得的星尘；快速 QA 仍不发放奖励或修改生涯统计。
- 清除本地记录现在同时重置机库解锁、装备与星尘经济。

### 验证

- 使用本地测试钱包验证连续解锁、精确扣款、装备切换、刷新持久化和一键清档恢复默认配置。
- 脉冲 MK-III + 共振透镜完整通过三章快速战役：结束时两机 6/6 耐久、60 FPS、`QA CAMPAIGN CLEAR`。
- 壁垒 MK-II + 泡泡电容正确以 9/9 耐久、1 层护盾、79 速度和 0.9 射速系数进入战斗。
- 两种配置的独立 3D 几何均完成视觉检查；浏览器日志为空。
- JavaScript 语法检查和正式依赖审计通过；Windows x64 EXE/ZIP 已按 0.5.0 重建并校验。

## 0.4.0 — 战役事件与 Boss 阶段

### 新增

- 三章共九种命名事件波次，包含楔形突击、交叉火线、运输护航、旋翼阵、雷区和钳形包围等编队。
- 每章加入一场有独立光环、耐久、火力与积分奖励的 3D 精英战。
- 三名 Boss 均加入三阶段招式、阶段护盾、受击闪白、弹幕清场、镜头推进和阶段名称 HUD。
- 设置页加入高对比敌方弹幕选项；所有交互控件补充键盘焦点轮廓。
- 从 SVG 母版生成 16、24、32、48、64、128、256px 的 Windows ICO，并同步输出 PNG 与 ICNS。
- 加入仅限 localhost 的三章快速冒烟模式和 Canvas 诊断属性，且测试结果不会写入生涯存档。

### 改动

- Boss 登场时清除残留杂兵，避免上一事件与 Boss 弹幕叠加造成非预期难度尖峰。
- 敌机侧向速度现在会真实作用于运动，侧翼和钳形编队不再退化成直线下落。
- 精英和 Boss 受击、阶段转换、屏幕震动及音效反馈重新分层。
- Windows CI 在打包前运行语法检查与正式依赖审计。

### 验证

- JavaScript 语法检查通过，正式运行依赖审计为 0 个漏洞。
- 正常速度验证第一章三个事件、精英战、Boss 三阶段与第二章转场；单人 AI 可完成救援。
- localhost 快速冒烟模式完整通过三章并显示 `QA CAMPAIGN CLEAR`，结束时两机仍存活，稳定采样为 60 FPS。
- WebGL 浏览器控制台无日志、警告或错误；快速测试不会污染生涯统计。
- Windows x64 免安装目录、EXE 与无 `__MACOSX` 的 ZIP 已重新生成并通过归档完整性检查。

## 0.3.0 — 单人僚机与产品化界面

### 新增

- 单人 + AI 僚机与本地双人两种模式；模式选择会保存在本机。
- 僚机 AI 可预判弹道并闪避、保持共振距离、寻找集火目标，并在 P1 坠毁后主动救援。
- 首次出航三步教学、DOM 暂停菜单、设置面板和返回主菜单流程。
- 音乐与音效独立音量、三档 3D 渲染质量、屏幕震动与闪光强度设置。
- 本地生涯存档：最高分、累计击破、最佳连击、出航、通关和航行时间。
- 手柄热插拔提示与命中、护盾、拾取、超新星分级震动反馈。

### 改动

- 暂停界面从 Canvas 文本升级为可操作、可访问的矢量 DOM 菜单。
- 单人 HUD 会明确显示 AI 身份，开局提示会反馈当前模式。
- 3D 渲染分辨率可根据性能、平衡、高清档动态调整。
- 尊重系统“减少动态效果”偏好，新存档默认降低震动与闪光。
- 设置、模式和统计均在离线与 Electron 文件协议下安全降级。

### 验证

- JavaScript 语法检查通过，正式运行依赖审计为 0 个漏洞。
- 浏览器验证模式切换、首次教学、设置持久化、飞行中暂停/恢复与章节转场；控制台无日志、警告或错误。
- 单人无人操作 14 秒时两机均保持满血；持续测试已击破第一章 Boss 并正常进入第二章。
- Electron Forge 成功生成 Windows x64 桌面目录与 `SPACECRAFT.exe`，发行 ZIP 完整性检查通过。

## 0.2.0 — WebGL 3D 重制

### 新增

- 原生 WebGL 3D 渲染器：透视相机、深度缓冲、法线光照与距离雾。
- 3D 飞船、五类敌机、三种 Boss、弹体、护盾、掉落物与粒子几何体。
- 三章独立 3D 地标：糖果星球与行星环、云端齿轮工厂、终焉城堡。
- 多轨 8-bit 音序器：旋律、复调、琶音、低音、和弦、底鼓、军鼓与镲片。
- 分层射击、命中、敌机发射、护盾、爆炸、复活、Boss 和过关音效。
- Electron Forge 桌面打包工程和 Windows x64 CI 构建流程。
- Windows x64 免安装版 `SPACECRAFT.exe` 与 ZIP 发行包。
- 全部页面功能图标改为内联 SVG。

### 改动

- 操作简化为 P1 `WASD`、P2 方向键；射击、技能、共振和救援自动触发。
- 第一章加入 6 秒敌方停火窗口。
- 延长刷怪间隔并设置同时存活敌人上限。
- 敌方弹速降低约 30%–45%，Boss 弹幕数量与频率重新平衡。
- 玩家生命提高至 7，缩小受击核心，受击无敌时间延长至 1.8 秒。
- 坠毁时清除附近弹体，救援时间缩短至 1.65 秒，救援窗口延长至 12 秒。

### 验证

- JavaScript 语法检查通过。
- 浏览器 WebGL 着色器编译和链接通过。
- 已验证标题页、自动开火、首章刷怪、3D 深度关系、SVG 控件与暂停流程。
- 浏览器控制台无错误或警告。
- Electron macOS arm64 桌面包和 Windows x64 可执行目录均已成功生成。
- 正式运行依赖审计：0 个漏洞。

## 0.1.0 — 原型重做

- 建立三章双人合作射击原型。
- 加入实时 8-bit 配乐、Boss、升级、救援、连击与共振机制。
