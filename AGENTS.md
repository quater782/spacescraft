# SPACECRAFT Codex 指南

本文件是仓库级工作约定。进入子目录时，继续遵守距离目标文件最近的 `AGENTS.md`。

## 产品不可变约束

- 游戏是卡通像素风的真实 WebGL 3D 双人合作射击，不得退化为仅用 2D 缩放模拟深度。
- 玩家只负责方向移动；射击、能量技能、共振和近距离救援自动触发。不要增加必须手动按键的战斗技能。
- 单人模式必须有可存活、会集火和救援的 AI 僚机；本地双人支持 P1 WASD、P2 方向键及双手柄。
- 前期必须可学习、可通过。改变刷怪、敌弹速度、伤害、碰撞或 Boss 火力时，必须验证第一章正常速度体验。
- 用户界面功能图标使用 SVG 或代码生成矢量几何；不要用会因放大而模糊的位图图标。桌面应用图标可由矢量母版生成多分辨率位图。
- 音乐与音效由 Web Audio 实时合成，保持多层 8-bit 风格；新反馈应有可辨识的音高、包络或节奏层，而不是重复单一音效。
- 所有玩家可见文本、动态名称、ARIA 名称与提示同时支持简体中文和英文。

## 开工前读取

1. 阅读[文档中心](./docs/README.md)和[当前项目状态](./docs/PROJECT-STATE.md)。
2. 阅读与任务直接相关的专题文档及最近一版[更新日志](./CHANGELOG.md)。
3. 检查 `git status --short`，保留用户已有修改，不覆盖无关文件。
4. 为本轮确定一个清晰的 Major 更新主题；功能迭代应围绕该主题形成可验证闭环，避免无关零碎改动。

## 源码与事实来源

- `src/game.js`：固定时间步、输入、关卡、AI、碰撞、局内构筑、存档、Web Audio 与 HUD。
- `src/renderer3d.js`：WebGL 相机、着色器、网格、灯光、雾和场景绘制。
- `src/i18n.js`：中英文文案目录和语言切换。
- `index.html` / `style.css`：DOM 结构、可访问语义和响应式视觉系统。
- `electron/main.cjs` / `forge.config.cjs`：桌面安全边界和发行配置。
- `docs/PROJECT-STATE.md`：发行基线、开发快照和已知差距；版本冲突以此为入口核对源码与产物。

不要直接编辑 `node_modules/`、`out/` 或 `dist/`。它们是依赖或生成物，不是源文件。

## 实施规则

- 保持 `STEP = 1 / 60` 的固定时间步语义；新增时间相关逻辑使用传入的 `dt`，不要绑定显示器刷新率。
- 数据使用稳定 ID，展示层使用 i18n key。新增或修改文案时同步两种语言，并运行完整验证。
- 修改存档结构时提升 profile 版本、合并安全默认值、保留旧字段迁移，并验证损坏/缺失数据能回退。
- QA 参数只能在 localhost/127.0.0.1 生效，不得改变正式域名或 Electron 文件协议行为。
- 保留 `#game` 的诊断 `data-*` 属性；新增系统应补充最小、稳定、可机器读取的状态。
- Electron 生产配置默认离线、最小权限；除非任务明确需要，不引入远程内容、遥测或新运行时依赖。
- 不把未执行的测试写成已通过，不把旧发行包或旧哈希描述为当前版本产物。

## 验证与完成标准

每次代码或文档变更至少运行：

```bash
npm run verify
```

游戏逻辑、视觉或交互变化还应按[开发与验证](./docs/DEVELOPMENT.md)进行浏览器实机检查；桌面边界变化运行 `npm start`，发行相关变化按[打包指南](./docs/PACKAGING.md)构建。若环境无法执行某项检查，交付时明确写出未验证项。

一次更新只有同时满足以下条件才算完成：

- Major 主题对应的用户路径形成闭环，没有已知阻断错误。
- 3D、方向操作、自动战斗、前期难度、双语、矢量图标和存档兼容约束未回归。
- `npm run verify` 通过，相关实机路径已检查或明确记录缺口。
- `CHANGELOG.md` 与受影响的专题文档同步；版本发布还要更新发行说明、打包记录和 `PROJECT-STATE.md`。

## Code Review Rules

审查优先级依次是：可玩性阻断与存档丢失、Electron 安全回归、3D/只移动/自动战斗产品回归、中英缺键与不可访问交互、性能和难度尖峰、文档或版本声明失真。发现问题时给出文件和可复现路径；不要只做风格评论。

完整操作流程见 [Codex 工作流](./docs/CODEX-WORKFLOW.md)。
